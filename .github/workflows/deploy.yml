name: Deploy Blue-Green

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: ⬇️ Checkout Code
        uses: actions/checkout@v3

      # 📜 1. AGREGAR HOST AL known_hosts (ROBUSTO)
      - name: 📜 Add Known Host
        run: |
          # 🚨 USANDO LA IP EXTERNA DE GCP: 34.127.35.52
          mkdir -p ~/.ssh 
          chmod 700 ~/.ssh
          ssh-keyscan -H 34.127.35.52 >> ~/.ssh/known_hosts

      # 🚀 2. DESPLIEGUE AUTOMÁTICO Y SWITCH
      - name: 🚀 Deploy & Switch (Alternancia Blue-Green)
        uses: appleboy/ssh-action@v1.0.1
        with:
          host: 34.127.35.52
          username: jennifer13br2004
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script_stop: true
          timeout: 1200s
          script: |
            echo "Iniciando pull y despliegue automatizado..."
            cd ~/Estrategia-Blue---Green
            git fetch origin
            git reset --hard origin/main
            git pull origin main
            
            # Ejecuta el script maestro que se encarga de TODO
            bash scripts/deploy.sh || exit 1
            
            echo "Despliegue y Switch completado con éxito."

      # 🔄 3. SWITCH DE TRAFICO Y ACTUALIZACIÓN DE ESTADO EN .ENV
      - name: 🔄 Switch Traffic to Green (Update Router and .env)
        uses: appleboy/ssh-action@v1.0.1
        with:
          host: 34.127.35.52
          username: jennifer13br2004
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script_stop: true
          script: |
            echo "Cambiando el tráfico al servidor Green..."
            cd ~/Estrategia-Blue---Green
            bash scripts/switch.sh green
            
            # 🚨 CORRECCIÓN: Apuntando a la ruta correcta 'front/.env'
            sed -i 's/^CURRENT_DEPLOYMENT=.*/CURRENT_DEPLOYMENT=green/' front/.env
            
            echo "Estado de CURRENT_DEPLOYMENT actualizado a green."
