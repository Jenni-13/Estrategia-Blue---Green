name: Deploy Blue-Green

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: ⬇️ Checkout Code
        uses: actions/checkout@v3

      # 🚀 1. DESPLIEGUE GREEN Y HEALTH CHECK (COMBINADO EN UN PASO)
      - name: 🚀 Deploy Green Server and Check Health
        uses: appleboy/ssh-action@v1.0.1 # Usando la versión más reciente
        with:
          # Parámetros de conexión
          host: 35.230.70.157
          username: jennifer13br2004
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          # 🚨 Importante: Evita la verificación de host.
          # Asegúrate de que esto solo se haga en entornos controlados,
          # o mejor, usa 'ssh-keyscan' en un paso previo.
          # Si usas known_hosts en un paso previo, puedes omitir 'insecure'
          script_stop: true
          # 📜 Script a ejecutar en el servidor remoto
          script: |
            echo "Iniciando pull en el repositorio..."
            cd ~/Estrategia-Blue---Green
            git pull origin main

            echo "Ejecutando despliegue Green con docker-compose..."
            bash scripts/deploy-green.sh || exit 1 # Falla si el script falla

            echo "Realizando verificación de salud (Health Check) en el servidor Green..."
            docker ps | grep frontend_green || exit 1
            curl -I http://localhost:3001 || exit 1
            echo "Health Check exitoso en el servidor Green."

      # 🔄 2. SWITCH DE TRAFICO
      - name: 🔄 Switch Traffic to Green (Cambio de Router)
        uses: appleboy/ssh-action@v1.0.1
        with:
          # Parámetros de conexión (Reutilizados)
          host: 35.230.70.157
          username: jennifer13br2004
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script_stop: true
          # 📜 Script para cambiar el tráfico
          script: |
            echo "Cambiando el tráfico al servidor Green..."
            cd ~/Estrategia-Blue---Green
            chmod +x scripts/switch.sh
            bash scripts/switch.sh green
