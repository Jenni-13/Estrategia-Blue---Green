name: Deploy Blue-Green

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: ⬇️ Checkout Code
        uses: actions/checkout@v3

      # 🔑 1. CONFIGURACIÓN SSH MANUAL
      - name: 🔑 Write Private Key and Add Host
        run: |
          mkdir -p ~/.ssh

          echo "${{ secrets.SSH_PRIVATE_KEY }}" | tr -d '\r' > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key

          ssh-keyscan -H 35.230.70.157 >> ~/.ssh/known_hosts

      # 🚀 2. DESPLIEGUE GREEN
      - name: 🚀 Deploy Green Server
        run: |
          ssh -i ~/.ssh/deploy_key jennifer13br2004@35.230.70.157 << 'EOF'
            echo "Iniciando pull en el repositorio..."
            cd ~/Estrategia-Blue---Green
            git pull origin main

            echo "Ejecutando despliegue Green con docker-compose..."
            bash scripts/deploy-green.sh || exit 1

            echo "Despliegue Green completado."
          EOF

      # 🩺 3. HEALTH CHECK GREEN
      - name: 🩺 Health Check Green Server
        run: |
          echo "Realizando verificación de salud (Health Check) en el servidor Green..."
          ssh -i ~/.ssh/deploy_key jennifer13br2004@35.230.70.157 << 'EOF'

            # Verificar que el contenedor correct esté arriba
            docker ps | grep frontend_green || exit 1

            # Health Check externo a la IP pública nueva del servidor Green
            curl -I http://localhost:3001 || exit 1

            echo "Health Check exitoso en el servidor Green."
          EOF

      - name: 🔄 Switch Traffic to Green (Cambio de Router)
        run: |
          echo "Cambiando el tráfico al servidor Green..."
          # Asegurarnos de que el script sea ejecutable
          chmod +x scripts/switch.sh
          # Ejecutar el script vía SSH
          ssh -i ~/.ssh/deploy_key jennifer13br2004@35.230.70.157 << 'EOF'
            bash scripts/switch.sh green
          EOF
