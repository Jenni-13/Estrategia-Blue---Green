name: Deploy Blue-Green

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    # Define un entorno para mejor seguimiento de secretos y despliegues
    environment: production

    steps:
      - name: ⬇️ Checkout Code
        uses: actions/checkout@v3

      # 🔑 1. Configurar la clave SSH usando el Agente 
      - name: 🔑 Setup SSH Agent
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

     # 🛡️ Add Server to Known Hosts
      - name: 🛡️ Add Server to Known Hosts
        run: |
          mkdir -p ~/.ssh
          # 🚨 ¡NUEVA IP CORREGIDA!
          ssh-keyscan -H 34.83.223.188 >> ~/.ssh/known_hosts

      - name: 🚀 Deploy Green Server
        run: |
          # 🚨 ¡NUEVA IP CORREGIDA!
          ssh jennifer13br2004@34.83.223.188 << 'EOF'
            # ... comandos ...
          EOF

      - name: 🩺 Health Check Green Server
        run: |
          # 🚨 ¡NUEVA IP CORREGIDA!
          ssh jennifer13br2004@34.83.223.188 << 'EOF'
            # ... comandos ...
          EOF

      - name: 🔄 Switch Traffic to Green (Cambio de Router)
        run: |
          # 🚨 ¡NUEVA IP CORREGIDA!
          ssh jennifer13br2004@34.83.223.188 << 'EOF'
            # ... comandos ...
          EOF
