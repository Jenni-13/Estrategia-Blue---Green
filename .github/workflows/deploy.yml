name: Deploy Blue-Green

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    # Define un entorno para mejor seguimiento de secretos y despliegues
    environment: production

    steps:
      - name: ⬇️ Checkout Code
        uses: actions/checkout@v3

    # 🔑 Setup SSH Agent
      - name: 🔑 Setup SSH Agent
        # 🟢 Usar la versión 0.7.0.
        # En algunos entornos, la v0.8.0 tiene problemas para cargar claves.
        uses: webfactory/ssh-agent@v0.7.0 
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
          # Usamos las opciones para asegurar que la clave se cargue
          ssh-add-options: '-k'

     # 🛡️ Add Server to Known Hosts
      - name: 🛡️ Add Server to Known Hosts
        run: |
          mkdir -p ~/.ssh
          # 🚨 ¡NUEVA IP CORREGIDA!
          ssh-keyscan -H 34.83.223.188 >> ~/.ssh/known_hosts

      - name: 🚀 Deploy Green Server
        run: |
          # 🚨 ¡NUEVA IP CORREGIDA!
          ssh jennifer13br2004@34.83.223.188 << 'EOF'
            # ... comandos ...
          EOF

      - name: 🩺 Health Check Green Server
        run: |
          # 🚨 ¡NUEVA IP CORREGIDA!
          ssh jennifer13br2004@34.83.223.188 << 'EOF'
            # ... comandos ...
          EOF

      - name: 🔄 Switch Traffic to Green (Cambio de Router)
        run: |
          # 🚨 ¡NUEVA IP CORREGIDA!
          ssh jennifer13br2004@34.83.223.188 << 'EOF'
            # ... comandos ...
          EOF
