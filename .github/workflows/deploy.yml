name: Deploy Blue-Green

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: ⬇️ Checkout Code
        uses: actions/checkout@v3

      # 📜 1. AGREGAR HOST AL known_hosts (ROBUSTO)
      - name: 📜 Add Known Host
        run: |
          # 🚨 USANDO LA IP EXTERNA DE GCP: 34.127.35.52
          mkdir -p ~/.ssh 
          chmod 700 ~/.ssh
          ssh-keyscan -H 34.127.35.52 >> ~/.ssh/known_hosts

      # 🚀 2. DESPLIEGUE GREEN Y HEALTH CHECK
      - name: 🚀 Deploy Green Server and Check Health
        uses: appleboy/ssh-action@v1.0.1
        with:
          # 🚨 IP ACTUALIZADA
          host: 34.127.35.52
          username: jennifer13br2004
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script_stop: true
          script: |
            echo "Iniciando pull en el repositorio..."
            cd ~/Estrategia-Blue---Green
            git fetch origin
            git reset --hard origin/main
            git pull origin main
            git pull origin main
            bash scripts/deploy-green.sh || exit 1
            echo "Realizando verificación de salud (Health Check)..."
            docker ps | grep frontend_green || exit 1
            curl -I http://localhost:3001 || exit 1
            echo "Health Check exitoso."

      # 🔄 3. SWITCH DE TRAFICO Y ACTUALIZACIÓN DE ESTADO EN .ENV
      - name: 🔄 Switch Traffic to Green (Update Router and .env)
        uses: appleboy/ssh-action@v1.0.1
        with:
          # 🚨 IP ACTUALIZADA
          host: 34.127.35.52
          username: jennifer13br2004
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script_stop: true
          script: |
            echo "Cambiando el tráfico al servidor Green..."
            cd ~/Estrategia-Blue---Green
            bash scripts/switch.sh green
            sed -i 's/^CURRENT_DEPLOYMENT=.*/CURRENT_DEPLOYMENT=green/' .env
            echo "Estado de CURRENT_DEPLOYMENT actualizado a green."
